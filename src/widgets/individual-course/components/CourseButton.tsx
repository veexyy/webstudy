export default function CourseButton(writeUserData: any) {
  return (
    <button
      onClick={() => writeUserData.writeUserData.writeUserData()}
      className="border border-white rounded-xl text-white px-6 py-2 font-montserrat font-bold text-xs sm:text-base"
    >
      Записаться
    </button>
  );
}
